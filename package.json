{
  "name": "beam-cli",
  "version": "0.1.0",
  "description": "Hand off a live coding-agent session (omp, Claude Code, Codex) to a remote sandboxed server — and bring it back.",
  "license": "MIT",
  "repository": "github:bipa-app/beam",
  "bin": {
    "beam": "./src/cli.ts"
  },
  "scripts": {
    "beam": "bun src/cli.ts",
    "style": "bun scripts/check-style.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.0"
  }
}
