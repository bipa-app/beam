// Beam's Rust port grows module by module against the parity goldens in
// parity/goldens/ (DESIGN.md: Rust port (transition record)). Module paths
// mirror the TypeScript sources they transliterate.

pub mod util;
